<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
   android:id="@+id/activity_main"
   xmlns:android="http://schemas.android.com/apk/res/android"
   xmlns:tools="http://schemas.android.com/tools"
   android:layout_width="match_parent"
   android:layout_height="match_parent"
   android:paddingBottom="@dimen/activity_vertical_margin"
   android:paddingLeft="@dimen/activity_horizontal_margin"
   android:paddingRight="@dimen/activity_horizontal_margin"
   android:paddingTop="@dimen/activity_vertical_margin"
   tools:context="com.llamacorp.filterdialogexample.MainActivity">

   <EditText
      android:id="@+id/edit_text_view"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_alignBaseline="@+id/country_text_view"
      android:layout_alignBottom="@+id/country_text_view"
      android:layout_alignParentEnd="true"
      android:layout_marginEnd="12dp"
      android:ems="10"
      android:inputType="text"/>

   <TextView
      android:id="@+id/country_text_view"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_marginTop="44dp"
      android:labelFor="@id/edit_text_view"
      android:text="@string/country"
      android:layout_below="@+id/list_view_dialog_button"
      android:layout_alignParentStart="true"/>

   <Button
      android:id="@+id/list_view_dialog_button"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:text="@string/list_of_countries"
      android:layout_marginTop="34dp"
      android:layout_alignParentTop="true"
      android:layout_centerHorizontal="true"/>

</RelativeLayout>